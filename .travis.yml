sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t PuttyByteCoding/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true PuttyByteCoding/docker-react npm run test
